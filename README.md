# PRiR Project
## Install nvcc (CUDA)
```
sudo apt update
sudo apt install -y nvidia-cuda-toolkit
```
## If nvcc (CUDA) is installed, the current version should appear
```
nvcc --version
```
## Compile a program:
```
make
```
## Run a program:
```
./PRiR_Projekt1
```
